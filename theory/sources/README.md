# Source provenance

`library.csv` 是统一文献台账，记录核验后的元数据、DOI/稳定页面、全文来源、证据状态、访问日期、本地文件、页数和 SHA-256。

`batch_01_06.csv`、`batch_07_11.csv`、`batch_12_16.csv` 与 `code_derived.csv` 是分批取证的原始台账，保留用于复核；跨批次检索和统计以 `library.csv` 为准。

`CAESAR_PLAN_2026-08-02.md` 是用户提交的架构提案原文归档，只是立法研究对象，不属于论文台账或现行法律。
